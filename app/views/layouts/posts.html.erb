<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Fotobook</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>
  <body class="d-flex flex-column min-vh-100">
    <%= render "layouts/nav" %>
    <%# Switch Buttons
    
    </div> %>
    <%# Main %>
    <div class="container-fluid posts">
      <div class="row h-100">
        <%# Sidebar %>
        <%= render "layouts/sidebar" %>
        <%# Main Content %>
        <div class="col-12 col-lg-8 pt-5 bg-white mt-2">
          <%= render "layouts/switch_button" %>
          <div class="py-3">
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
    <!-- Mobile Bottom Navigation - Visible only on mobile -->
    <div class="d-lg-none bg-light border-top fixed-bottom">
      <div class="container-fluid">
        <div class="row">
          <div class="col-6">
            <%= link_to feeds_photos_path, class: "btn btn-link w-100 py-3 text-decoration-none", id: "mobile-feeds-btn" do %>
              <div class="d-flex flex-column align-items-center">
                <i class="fas fa-home mb-1"></i>
                <small class="fw-bold">Feeds</small>
              </div>
            <% end %>
          </div>
          <div class="col-6">
            <%= link_to discover_photos_path, class: "btn btn-link w-100 py-3 text-decoration-none text-muted", id: "mobile-discover-btn" do %>
              <div class="d-flex flex-column align-items-center">
                <i class="fas fa-compass mb-1"></i>
                <small class="fw-bold">Discover</small>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
